# F-001 / F-011 — Phase 2 Report (Implementation + Verification)

**Date:** 2026-05-11
**Status:** Phase 2 complete. All 4 founder-facing surfaces aligned on year-aware Personnel % verdict. STOPPING per phase-gate discipline before F-006.

---

## Summary

Created single-source-of-truth helper for Personnel % health thresholds. Refactored 6 consumers. All 44 unit tests pass (18 R-ENR-01 + 26 new). End-to-end verified across Overview tile, Overview AI Briefing, Staffing Advisor agent, and Ask SchoolLaunch agent — all 4 surfaces now consistently identify Evergreen's 65.3% Y1 personnel as healthy founding-year.

## Files changed

| File | Change |
|---|---|
| **NEW** `src/lib/healthThresholds.ts` | Exports `evaluatePersonnelPctHealth(pct, year)`, `getPersonnelHealthBand(year)`, `personnelHealthBandsForPrompt()`. Two-band model: Y1 founding 65–72%, Y2+ steady-state 72–78%. Symmetric ±3pt approaching bands. |
| `src/app/(authenticated)/dashboard/page.tsx` | Imports helper. Replaced inline `<72 ? fails : <=78 ? meets : <=80 ? approaching : fails` ternary with `evaluatePersonnelPctHealth(pct, 1)`. Tile now passes `personnelHealth.short` as sublabel ("Within founding-year range"). |
| `src/app/(authenticated)/dashboard/staffing/page.tsx` | Imports helper. Replaced Personnel-% callout's hardcoded `>80` threshold + hardcoded "72-78%" copy with helper-driven verdict + helper's `long` explanation. Callout no longer fires for healthy founding-year schools. |
| `src/app/api/advisory/route.ts` | Imports `personnelHealthBandsForPrompt`. Prepended helper output to every agent's system prompt at the `callAnthropic` call site. Updated 5 inline string references in the Staffing Advisor prompt (lines 116, 129, 137, 144, 145) to remove hardcoded "72-78%" and instead refer to "the year-appropriate meets band" with explicit warning against citing steady-state as Y1 standard. |
| `src/app/api/chat/route.ts` | Imports helper. Prepended `personnelHealthBandsForPrompt()` to the WA-charter system prompt. Replaced the hardcoded "PERSONNEL SUSTAINABILITY — DEFINITIVE THRESHOLDS" block (lines 58-62) with year-aware language referencing the prepended bands. Generic-pathway prompt untouched per WA-only scope. |
| `src/app/api/export/narrative/route.ts` | Imports helper. Refactored `personnelAssessment()` to use `evaluatePersonnelPctHealth(pct, 1).verdict` + `.long`. Refactored the metric card (line 596-600) IIFE to use helper for background color, foreground color, and the "Target: ..." sub-label. |
| `src/app/dev/components/Showcase.tsx` | Added two-line FIXME comment per D7 — outlier `75–80%` text intentionally not synced; production lives in `healthThresholds.ts`. |
| **NEW** `tests/session4/health-thresholds.spec.ts` | 26 boundary tests covering Y1 (founding) and Y2+ (steady-state) bands at exact boundaries (62, 65, 72, 75, 69, 78, 81), Evergreen's 65.3% case, the 70% Y1-vs-Y3 differentiation, and `personnelHealthBandsForPrompt()` prompt-string contract tests. |

**Not touched (per scope discipline):**
- Generic pathway code (`computeGenericProjections`, `generic_*` prompts) — Generic pathway has its own staffing norms, out of scope.
- Days of Cash thresholds, Total Margin thresholds, Current Ratio thresholds, Enrollment Variance thresholds — backlogged as R-HEALTH-01 (extend the helper pattern to all health metrics).

## Verification — 44/44 tests pass

```
npx playwright test tests/session4/grade-expansion.spec.ts tests/session4/health-thresholds.spec.ts
44 passed (1.6s)
```

18 R-ENR-01 tests (unchanged) + 26 new F-001 tests.

## End-to-end browser verification (4 surfaces aligned)

### 1. Overview tile — `audit-13` → `audit-?` (current)

| Field | Pre-fix | Post-fix |
|---|---|---|
| status | `fails` (red) | `meets` (teal) |
| border-left-color | `var(--rose-500)` | `var(--teal-500)` |
| sublabel | (none) | "Within founding-year range" |

### 2. Overview AI Briefing

Regenerated post-cache-clear. Briefing now says: *"Your model meets all Stage 1 standards for Years 1-2 and all Stage 2 standards..."* — no longer surfaces Personnel % as a concern.

### 3. Staffing Advisor agent (Advisory Panel) — `audit-24-f001-fixed-advisory.png`

Agent quote: *"Evergreen Heights' Year 1 personnel percentage of 65.3% **falls within the healthy founding-year band (65-72%)**, but the school faces significant operational gaps that could impact quality and compliance."*

The agent still rightly flags OTHER concerns (Business Manager gap, ELL teacher, dual leadership redundancy) — but no longer incorrectly flags personnel %.

### 4. Ask SchoolLaunch — `audit-25-f001-fixed-ask.png`

User question: *"How does our personnel ratio compare to healthy WA charters?"*

Agent response: *"Your personnel spending looks healthy for a founding school. At 65.3% of operating revenue, you're right in the middle of the healthy range for Year 1 schools (65-72%). [...] **Year 1 (Founding) - Your Current Status:** Healthy range: 65-72%, Your school: 65.3% ✓ **MEETS STANDARD** [...] Looking Ahead: Once you reach steady-state operations (Year 2+), the healthy range shifts to 72-78%."*

180° flip from pre-fix response ("below the healthy range... 7 percentage points below the minimum threshold").

### Regression check — zero false-language returned

Body-text grep across the Advisory Panel and Ask SchoolLaunch responses for:
- `"below the healthy 72-78%"` — **0 matches** ✓
- `"below 72%"` (rigid steady-state framing applied to Y1) — **0 matches** ✓

Founding-year language now present:
- "founding-year band" / "founding year" — 5+ mentions across agents
- "65-72%" range — cited explicitly

## DB state

- `advisory_cache` nullified for 2 schools (Evergreen + 1 other) before re-verification
- Caches naturally regenerated on Overview + Advisory Panel visits
- All other schools' caches remain null until they're visited (per design — lazy cache repopulation)

## Decisions locked

Per Travis's Phase 1 approval:
- **D1:** Two bands (founding Y1, steady-state Y2+) ✓
- **D2:** 65% founding lower bound, no padding ✓
- **D3:** Symmetric approaching ±3pts each side ✓
- **D4:** No `schoolStatus` parameter ✓
- **D5:** `src/lib/healthThresholds.ts` ✓
- **D6:** Helper returns `{short, long}`; same text consumed across all surfaces ✓
- **D7:** Showcase outlier left alone with FIXME comment ✓
- **D8:** Cache nullified, agents regenerated, no obsolete `72-78%` strings in output ✓
- **Scope:** Personnel %-only (R-HEALTH-01 backlog for extending pattern) ✓

## Constraints honored

- WA pathway only (Generic pathway untouched)
- No git operations
- Project ref `nlvlrznhiwuorxlapnej` verified
- No scope creep — F-001 stays Personnel %-only

## STOP — phase-gate discipline before F-006

Per Travis's instructions: "Stop after F-001 verification before moving to F-006."

Awaiting greenlight to proceed to F-006 Phase 2 (one-line semantic fix at `budgetEngine.ts:553` plus Multi-Year row insertion, cross-surface verification, Total Margin trip-wire check, regression test).
